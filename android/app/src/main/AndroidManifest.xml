<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:label="RFID Sample"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        tools:replace="android:label">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />


        <!-- Required for Zebra RFID SDK3 on devices with EMDK service -->
        <uses-library
            android:name="com.symbol.emdk"
            android:required="false" />
    </application>

    <!-- DataWedge and EMDK service visibility -->
    <queries>
        <package android:name="com.symbol.datawedge" />
        <package android:name="com.symbol.emdk.emdkservice" />
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

    <!-- Bluetooth permissions – Android 12+ requires BLUETOOTH_SCAN + BLUETOOTH_CONNECT at runtime -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- EMDK permission for built-in RFID service on TC-series devices -->
    <uses-permission android:name="com.symbol.emdk.permission.EMDK" />

    <!-- External storage access for export/share (if needed in future) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- DataWedge content provider access -->
    <uses-permission android:name="com.symbol.datawedge.permission.contentprovider" />

    <!-- Zebra BarcodeScanner SDK service permissions -->
    <permission
        android:name="com.zebra.scannercontrol.SERVICE_LAUNCH_PERMISSION"
        android:protectionLevel="normal" />
    <permission
        android:name="com.zebra.scannercontrol.ACTIVITY_LAUNCH_PERMISSION"
        android:protectionLevel="normal" />

</manifest>
